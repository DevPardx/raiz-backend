export enum UserRole {
    BUYER = "buyer",
    SELLER = "seller",
}

export enum PropertyType {
    HOUSE = "house",
    APARTMENT = "apartment",
    LAND = "land",
    COMMERCIAL = "commercial",
    WAREHOUSE = "warehouse",
}

export enum PropertyStatus {
    ACTIVE = "active",
    PAUSED = "paused",
    SOLD = "sold",
}
